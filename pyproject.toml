[project]
name = "the-seed"
version = "0.1.0"
description = "LLM Agent framework"
requires-python = ">=3.10"
dependencies = [
    "flashtext>=2.7",
    "openai>=1.49.0",
    "rapidfuzz>=3.14.3",
    "websockets>=12.0",
]

[tool.setuptools.packages.find]
where = ["."]
